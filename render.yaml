services:
  - type: web
    name: novasave
    env: node
    buildCommand: "npm install && pip3 install yt-dlp"
    startCommand: "node server.js"
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
